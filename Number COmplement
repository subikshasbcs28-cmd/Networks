int findComplement(int num) {
    unsigned int mask = 1;
    
    // num oda binary length ku mask ready panrom
    while (mask < num) {
        mask = (mask << 1) | 1;
    }
    
    // XOR panni bits ah flip panrom
    return num ^ mask;
}
